<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="WslContainersDesktop_App.Pages.NetworksPage"
    x:Name="RootPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:conv="using:WslContainersDesktop_App.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:WslContainersDesktop_App.Pages"
    xmlns:tables="using:WslContainersDesktop_App.Tables"
    xmlns:vm="using:WslContainersDesktop_App.ViewModels"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <conv:AutomationIdConverter x:Key="AutomationIdConverter" />
    </Page.Resources>

    <Grid Padding="24,16,24,16" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border
            Grid.Row="0"
            Padding="16"
            Background="{ThemeResource LayerFillColorDefaultBrush}"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="{ThemeResource WslContainersSurfaceBorderThickness}"
            CornerRadius="{StaticResource OverlayCornerRadius}">
            <Grid ColumnSpacing="16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="4" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Border
                    Grid.Column="0"
                    Background="{ThemeResource WslContainersAccentFillColorDefaultBrush}"
                    CornerRadius="{StaticResource ControlCornerRadius}" />
                <StackPanel Grid.Column="1">
                    <TextBlock x:Name="TxtNetworksPageTitle" x:Uid="NetworksPageTitle" Style="{StaticResource TitleTextBlockStyle}" />
                    <TextBlock x:Name="TxtNetworksPageDescription" x:Uid="NetworksPageDescription" />
                </StackPanel>
                <Button
                    x:Name="BtnRefreshNetworks"
                    x:Uid="BtnRefreshNetworks"
                    Grid.Column="2"
                    VerticalAlignment="Bottom"
                    AutomationProperties.AutomationId="BtnRefreshNetworks"
                    Command="{x:Bind ViewModel.RefreshCommand}" />
            </Grid>
        </Border>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="8">
            <TextBox
                x:Name="TxtNewNetworkName"
                x:Uid="TxtNewNetworkName"
                Width="320"
                AutomationProperties.AutomationId="TxtNewNetworkName"
                IsEnabled="{x:Bind ViewModel.CanCreate, Mode=OneWay}"
                Text="{x:Bind ViewModel.NewNetworkName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <Button
                x:Name="BtnCreateNetwork"
                x:Uid="BtnCreateNetwork"
                VerticalAlignment="Bottom"
                AutomationProperties.AutomationId="BtnCreateNetwork"
                Command="{x:Bind ViewModel.CreateCommand}"
                IsEnabled="{x:Bind ViewModel.CanCreate, Mode=OneWay}" />
            <ProgressRing
                x:Name="PrgCreateNetwork"
                Width="24"
                Height="24"
                VerticalAlignment="Bottom"
                AutomationProperties.AutomationId="PrgCreateNetwork"
                IsActive="{x:Bind ViewModel.IsCreating, Mode=OneWay}"
                Visibility="{x:Bind ToVisibleWhenTrue(ViewModel.IsCreating), Mode=OneWay}" />
        </StackPanel>

        <StackPanel Grid.Row="2" Spacing="8">
            <InfoBar
                x:Name="InfoBarNetworkError"
                Severity="Error"
                IsClosable="False"
                IsOpen="{x:Bind ViewModel.IsErrorMessageVisible, Mode=OneWay}"
                Message="{x:Bind ViewModel.ErrorMessage, Mode=OneWay}" />
            <InfoBar
                x:Name="InfoBarNetworkStatus"
                Severity="Success"
                IsClosable="False"
                IsOpen="{x:Bind ViewModel.IsStatusMessageVisible, Mode=OneWay}"
                Message="{x:Bind ViewModel.StatusMessage, Mode=OneWay}" />
        </StackPanel>

        <Grid Grid.Row="3" RowSpacing="8">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <InfoBar
                x:Name="InfoBarNoUserNetworks"
                x:Uid="InfoBarNoUserNetworks"
                Grid.Row="0"
                Severity="Informational"
                IsClosable="False"
                IsOpen="{x:Bind ViewModel.IsNoUserNetworksInfoVisible, Mode=OneWay}" />

            <Grid Grid.Row="1">
                <Border
                    Background="{ThemeResource LayerFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="{ThemeResource WslContainersSurfaceBorderThickness}"
                    CornerRadius="{StaticResource OverlayCornerRadius}"
                    Visibility="{x:Bind ToVisibleWhenTrue(ViewModel.HasNetworks), Mode=OneWay}">
                    <Grid RowSpacing="4">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <Grid
                            Grid.Row="0"
                            Padding="12,8"
                            ColumnSpacing="12"
                            HorizontalAlignment="Stretch">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="120" />
                            </Grid.ColumnDefinitions>

                            <Grid
                                Grid.Column="0"
                                tables:ClipToBoundsBehavior.IsEnabled="True"
                                tables:TableLayoutBehavior.Layout="{StaticResource NetworksTableColumnLayout}"
                                tables:TableLayoutBehavior.Role="Header">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="180" tables:TableLayoutBehavior.ColumnIndex="0" />
                                    <ColumnDefinition Width="8" />
                                    <ColumnDefinition Width="120" tables:TableLayoutBehavior.ColumnIndex="1" />
                                    <ColumnDefinition Width="8" />
                                    <ColumnDefinition Width="180" tables:TableLayoutBehavior.ColumnIndex="2" />
                                    <ColumnDefinition Width="8" />
                                    <ColumnDefinition Width="96" tables:TableLayoutBehavior.ColumnIndex="3" />
                                    <ColumnDefinition Width="8" />
                                    <ColumnDefinition Width="120" tables:TableLayoutBehavior.ColumnIndex="4" />
                                    <ColumnDefinition Width="8" />
                                    <ColumnDefinition Width="*" tables:TableLayoutBehavior.ColumnIndex="5" />
                                </Grid.ColumnDefinitions>
                                <TextBlock x:Name="TxtNetworkColumnName" x:Uid="TxtNetworkColumnName" Grid.Column="0" FontWeight="SemiBold" />
                                <tables:TableColumnSplitter
                                    x:Uid="SpltNetworksNameDriver"
                                    Grid.Column="1"
                                    HorizontalAlignment="Stretch"
                                    VerticalAlignment="Stretch"
                                    AutomationProperties.AutomationId="SpltNetworksNameDriver" />
                                <TextBlock x:Name="TxtNetworkColumnDriver" x:Uid="TxtNetworkColumnDriver" Grid.Column="2" FontWeight="SemiBold" />
                                <tables:TableColumnSplitter
                                    x:Uid="SpltNetworksDriverCreated"
                                    Grid.Column="3"
                                    HorizontalAlignment="Stretch"
                                    VerticalAlignment="Stretch"
                                    AutomationProperties.AutomationId="SpltNetworksDriverCreated" />
                                <TextBlock x:Name="TxtNetworkColumnCreatedAt" x:Uid="TxtNetworkColumnCreatedAt" Grid.Column="4" FontWeight="SemiBold" />
                                <tables:TableColumnSplitter
                                    x:Uid="SpltNetworksCreatedContainers"
                                    Grid.Column="5"
                                    HorizontalAlignment="Stretch"
                                    VerticalAlignment="Stretch"
                                    AutomationProperties.AutomationId="SpltNetworksCreatedContainers" />
                                <TextBlock x:Name="TxtNetworkColumnContainers" x:Uid="TxtNetworkColumnContainers" Grid.Column="6" FontWeight="SemiBold" />
                                <tables:TableColumnSplitter
                                    x:Uid="SpltNetworksContainersType"
                                    Grid.Column="7"
                                    HorizontalAlignment="Stretch"
                                    VerticalAlignment="Stretch"
                                    AutomationProperties.AutomationId="SpltNetworksContainersType" />
                                <TextBlock x:Name="TxtNetworkColumnType" x:Uid="TxtNetworkColumnType" Grid.Column="8" FontWeight="SemiBold" />
                                <tables:TableColumnSplitter
                                    x:Uid="SpltNetworksTypeUsage"
                                    Grid.Column="9"
                                    HorizontalAlignment="Stretch"
                                    VerticalAlignment="Stretch"
                                    AutomationProperties.AutomationId="SpltNetworksTypeUsage" />
                                <TextBlock x:Name="TxtNetworkColumnUsage" x:Uid="TxtNetworkColumnUsage" Grid.Column="10" FontWeight="SemiBold" />
                            </Grid>

                            <StackPanel Grid.Column="1" HorizontalAlignment="Right" Orientation="Horizontal" />
                        </Grid>

                        <ListView
                            x:Name="LstNetworks"
                            Grid.Row="1"
                            AutomationProperties.AutomationId="LstNetworks"
                            ItemContainerStyle="{StaticResource TableListViewItemStyle}"
                            ItemsSource="{x:Bind ViewModel.Networks, Mode=OneWay}"
                            SelectionMode="None">
                            <ListView.ItemTemplate>
                                <DataTemplate x:DataType="vm:NetworkRowViewModel">
                                    <Grid Padding="12,8" ColumnSpacing="12" HorizontalAlignment="Stretch">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="120" />
                                        </Grid.ColumnDefinitions>

                                        <Grid
                                            Grid.Column="0"
                                            tables:ClipToBoundsBehavior.IsEnabled="True"
                                            tables:TableLayoutBehavior.Layout="{StaticResource NetworksTableColumnLayout}"
                                            tables:TableLayoutBehavior.Role="Row">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="180" tables:TableLayoutBehavior.ColumnIndex="0" />
                                                <ColumnDefinition Width="8" />
                                                <ColumnDefinition Width="120" tables:TableLayoutBehavior.ColumnIndex="1" />
                                                <ColumnDefinition Width="8" />
                                                <ColumnDefinition Width="180" tables:TableLayoutBehavior.ColumnIndex="2" />
                                                <ColumnDefinition Width="8" />
                                                <ColumnDefinition Width="96" tables:TableLayoutBehavior.ColumnIndex="3" />
                                                <ColumnDefinition Width="8" />
                                                <ColumnDefinition Width="120" tables:TableLayoutBehavior.ColumnIndex="4" />
                                                <ColumnDefinition Width="8" />
                                                <ColumnDefinition Width="*" tables:TableLayoutBehavior.ColumnIndex="5" />
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Grid.Column="0" VerticalAlignment="Center" Text="{x:Bind Name}" />
                                            <TextBlock Grid.Column="2" VerticalAlignment="Center" Text="{x:Bind Driver}" />
                                            <TextBlock Grid.Column="4" VerticalAlignment="Center" Text="{x:Bind CreatedAtText}" />
                                            <TextBlock Grid.Column="6" VerticalAlignment="Center" Text="{x:Bind ConnectedContainerCountText}" />
                                            <TextBlock Grid.Column="8" VerticalAlignment="Center" Text="{x:Bind TypeText}" />
                                            <TextBlock Grid.Column="10" VerticalAlignment="Center" Text="{x:Bind UsageText}" />
                                        </Grid>

                                        <StackPanel Grid.Column="1" HorizontalAlignment="Right" Orientation="Horizontal">
                                            <Button
                                                x:Name="BtnDeleteNetwork"
                                                x:Uid="BtnDeleteNetwork"
                                                VerticalAlignment="Center"
                                                AutomationProperties.AutomationId="{x:Bind Name, Mode=OneWay, Converter={StaticResource AutomationIdConverter}, ConverterParameter=BtnDeleteNetwork}"
                                                CommandParameter="{x:Bind}"
                                                Click="BtnDeleteNetwork_Click"
                                                IsEnabled="{x:Bind CanDelete, Mode=OneWay}" />
                                        </StackPanel>
                                    </Grid>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </Grid>
                </Border>

                <Border
                    x:Name="BorderNetworksEmptyState"
                    Background="{ThemeResource LayerFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="{ThemeResource WslContainersSurfaceBorderThickness}"
                    CornerRadius="{StaticResource OverlayCornerRadius}"
                    Visibility="{x:Bind ToVisibleWhenFalse(ViewModel.HasNetworks), Mode=OneWay}">
                    <TextBlock
                        x:Name="TxtNetworksEmptyState"
                        x:Uid="TxtNetworksEmptyState"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center" />
                </Border>
            </Grid>
        </Grid>
    </Grid>
</Page>
